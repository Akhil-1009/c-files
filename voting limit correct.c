#include<stdio.h>
#include<conio.h>
void main()
{int age;
 printf ("enter your age");
 scanf ("%d",&age);
 if (age>=18)
     { printf ("you are eligible to vote");
	   }
	 else if (age>14)
	 
	   {printf ("you are tenager age to compete");
	   }
	   else
	     {printf ("you are not eligible to vote");
		 }
	   getch();
	   
}
